# JaldiKhana – API keys (copy to .env.local and fill in)
# The app uses the /api/generate route, so these server-side keys are used (recommended).

# Fallback priority: Gemini → Mistral → OpenAI → Grok
# You only need ONE key, but multiple keys provide better fallback support.

GEMINI_KEY=
MISTRAL_API_KEY=
OPENAI_API_KEY=
GROK_KEY=

# Alternative: if you call AI from the client, use these (exposes keys in the browser).
# NEXT_PUBLIC_GEMINI_KEY=
# NEXT_PUBLIC_MISTRAL_API_KEY=
# NEXT_PUBLIC_OPENAI_API_KEY=
# NEXT_PUBLIC_GROK_KEY=

# Free tier APIss:
# - GEMINI_KEY: https://aistudio.google.com/app/apikey (free, no limits)
# - MISTRAL_API_KEY: https://console.mistral.ai/ (free tier available)
# - OPENAI_API_KEY: https://platform.openai.com/api-keys (paid, has free credits)
# - GROK_KEY: https://console.x.ai/ (requires credits/license)
